language: go
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in the settings page of your repository, as a secure variable
  keep-history: true
  script: go get -t -v ./...
  script: go test -v ./...
  on:
    branch: master
